{
  "name": "baggage-tracker",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c auto \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
